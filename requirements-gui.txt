# Playlist Generator GUI Dependencies
# Install with: pip install -r requirements-gui.txt

# Qt framework for native Windows GUI
PySide6>=6.5.0

# Configuration
pyyaml>=6.0

# Platform-specific paths (for presets, logs)
platformdirs>=3.0

# Build tools (optional, for packaging)
# pyinstaller>=5.0
